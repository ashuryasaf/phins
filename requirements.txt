# Runtime dependencies for PHINS

# Database persistence (SQLite + PostgreSQL)
SQLAlchemy>=2.0.0
psycopg[binary]>=3.2.0

# HTTP client (market data service, uploads)
requests>=2.31.0

# PDF generation utilities
reportlab>=4.0.0

# S3 uploads (CI tooling / optional)
boto3>=1.34.0

# Encryption utilities (optional but recommended)
cryptography>=45.0.0
