{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3.12-venv > /dev/null 2>&1; python3.12 -m venv .venv && . .venv/bin/activate && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt"
}
