services:
  - type: web
    name: phins-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m web_portal.server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 8000
